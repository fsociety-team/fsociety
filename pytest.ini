[pytest]
testpaths = tests
addopts = --tb=short -v --html=playwright-report/report.html --self-contained-html
markers =
    non_regression: Tests de non-régression (anomalies corrigées lors de l'Atelier 2)
    functional: Tests fonctionnels (cas usuels)
